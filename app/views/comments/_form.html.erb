<%= form_for([topic,comment], remote: true) do |f|%>
  <%= f.hidden_field :topic_id %>
  <%= f.text_field :content, placeholder: "コメントを記載", class: "form-control"  %>
  <%= f.submit "登録", class:"comment-form-btn btn btn-primary btn-outline" %>
<% end %>
